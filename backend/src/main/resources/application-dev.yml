spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/lab_system?sslmode=disable
    username: lab_admin
    password: lab_password_123
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
